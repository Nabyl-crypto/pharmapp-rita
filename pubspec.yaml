name: pharmapp_rita
description: Application mobile & web géolocalisée pour la Pharmacie Rita.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  geolocator: ^11.0.0
  provider: ^6.1.1

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
